<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Playlist Size Tracker for Spotify</title>
</head>
<body>
    <h1><%= list_name %></h1><br>

    <% for (var i = 0; i < displayData.length; i++) { %>
    <pre><%= (i+1).toString().padStart(4, " ").padEnd(6, " ") %> | <%= displayData[i].name %> - <%= displayData[i].artists %> : <%= displayData[i].date %></pre>
    <% } %>
</body>
</html>